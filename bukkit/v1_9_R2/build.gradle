dependencies {
    compile (project(':bukkit')) {
        exclude group: 'org.spigotmc'
        exclude group: 'org.bukkit'
    }

    compileOnly group: 'org.spigotmc', name: 'spigot', version: '1.9.4-R0.1-SNAPSHOT'
}